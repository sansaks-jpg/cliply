{
  "name": "clip-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node frontend/scripts/start-backend.js && npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  }
}
