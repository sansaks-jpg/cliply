## 2026-06-22 - [Accessible Collapsible Settings Panel]\n**Learning:** Implementing `aria-expanded` and `aria-controls` on a toggle button alongside setting the target div's uid=1001(jules) gid=1001(jules) groups=1001(jules),27(sudo),103(docker) correctly establishes the relationship between a toggle and a collapsible panel. Without this, screen readers do not announce whether the advanced settings are open or closed.\n**Action:** Always link toggle buttons and their target panels using these ARIA attributes to ensure clear state communication to users relying on assistive technologies.
